enabled: true

detection:
  max-distance: 8.0
  scan-period-ticks: 2
  position-threshold: 0.02
  rotation-threshold-degrees: 1.0

focus:
  dwell-millis: 200
  lost-grace-millis: 300

render:
  mode: action-bar
  soft-limit: 96
  max-length: 120
  keepalive-millis: 2500

performance:
  idle-probe-ticks: 10

debug:
  enabled: false
